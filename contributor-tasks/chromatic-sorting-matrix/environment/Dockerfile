### 4. `environment/Dockerfile`
FROM python:3.11-slim

WORKDIR /task

# Copy the dataset generated by generate_conveyor.py into the container
COPY data/ /task/

# The agent must write its output to /task/output.json
CMD ["bash"]